as file:
    #     days = json.load(file)
    # final = {p: [0] *  for p in stations}
    # for month in List:
    #     for day in List[month]:
    #         day_str = day if int(day) >= 10 else '0' + day